---

openssh__packages:
  - openssh
  - openssh-server
  - openssh-clients

openssh__selinux_packages:
  - policycoreutils-python-utils

openssh__sshd_host_keys_dir: '/etc/ssh/'

openssh__sshd_subsystems:
  sftp: '/usr/libexec/openssh/sftp-server'
